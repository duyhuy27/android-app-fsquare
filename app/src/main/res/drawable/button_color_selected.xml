<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/primary" /> <!-- Màu nền khi được chọn -->
    <stroke
        android:width="2dp"
        android:color="@color/primary" /> <!-- Viền -->
    <corners android:radius="15dp" /> <!-- Bo góc -->
</shape>

